import { LandingLayout } from '@/components/layout/landing-layout';

export default function RVCamperSupportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <LandingLayout>{children}</LandingLayout>;
}
